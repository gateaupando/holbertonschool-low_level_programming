#include <stdio.h>
/**
 * main - script used to print a message without using puts
 * Description : description programme
 * Return: always 0 (succes)
 */
int main (void)
{
	printf(\"with proper grammar , but the outcome is a piece of art,\");
	return (0);
}
