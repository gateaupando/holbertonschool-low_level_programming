#include <stdlib.h>
#include <stdio.h>

/**
  * add - Entry point
  * @a : firt int to return
  * @b: second int to return
  * Return: Always 0 (Success)
*/
int add(int a, int b)
{
int somme = a + b;
return (somme);
}
